----------------------------------------
-- Get global key bindings --
----------------------------------------

return {
    global = require('keybindings.global'),
		client = require('keybindings.client')
}
